import { createTheme } from "@mui/material";

export const theme_input = createTheme({
    palette: {
        primary: {
            main: '#036564',
        },
        error:{
            main: '#FF3F42',
        }
    },
});

export const theme_paypal = createTheme({
    palette: {
        primary: {
            main: '#003087',
        },
        error:{
            main: '#FF3F42',
        }
    },
});

export const theme_visa = createTheme({
    palette: {
        primary: {
            main: '#1a1f71',
        },
        error:{
            main: '#FF3F42',
        }
    },
});
